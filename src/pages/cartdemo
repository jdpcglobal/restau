<b>₹{
        (subtotalWithD - 
          (
            appliedCoupon 
              ? 0 // Subtotal becomes 0 if coupon is applied
              : subtotalWithD // If no coupon, show regular subtotal
          ) - 
          (appliedCoupon 
            ? (subtotalWithD * appliedCoupon.rate / 100) // Apply coupon discount if available
            : 0)  + 
         deliveryFee + 
         totalGst
        ).toFixed(2)
      }</b>